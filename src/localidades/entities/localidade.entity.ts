import { Mask } from '../../mask/mask.entity';

export class Localidade extends Mask {
  //@OneToMany(() => User, (user) => user.localidad)
  //user: User;
  //@OneToMany(() => ProvinciaLocalidad, (provLoc) => provLoc.localidad)
  //provincia: ProvinciaLocalidad[];
}
