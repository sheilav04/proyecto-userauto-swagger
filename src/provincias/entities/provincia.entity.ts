import { Mask } from '../../mask/mask.entity';

export class Provincia extends Mask {
  //@OneToMany(() => User, (user) => user.provincia)
  //user: User;
  //@OneToMany(() => ProvinciaLocalidad, (provLoc) => provLoc.provincia)
  //localidad: ProvinciaLocalidad[];
}
